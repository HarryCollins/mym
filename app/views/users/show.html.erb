<h2><%= "#{@user.firstname} #{@user.secondname}" %> <small><%= "#{@user.username}" %></small> </h2>
<br>
<br>

<%= render 'form' %>

<%= link_to "Your markets", markets_path(joined: true), class: "btn btn-info" %>
<br>
