
<p class="text-center"><%= link_to "New User", "/register", class: "btn btn-primary" %></p>

<div class="row">
	<div class="col-md-6 col-md-offset-3">	
		<h2>Users</h2>

		<% @users.each do |user| %>
			<div class="panel">
				<%= link_to "#{user.username}", user_path(user), class: "h4" %>
			</div>
		<% end %>

	</div>
</div>