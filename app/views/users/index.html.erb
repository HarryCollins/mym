
<p class="text-center"><%= link_to "New User", "/register", class: "btn btn-primary" %></p>

<% @users.each do |user| %>
	<div class="panel">
		<%= link_to "#{user.firstname} #{user.secondname}", user_path(user) %>
	</div>
<% end %>