
<div class= "container">
  <div class="row">
    <div class= "col-md-8 col-md-offset-2">
        <h3><%= @market.name %><small> Market <%= @market.market_status.market_status %></small></h3>
            <%= link_to "Enter Market Results", results_form_market_path(@market), class:"btn btn-info btn-xs pull-right" %>
            <br>
            <%= link_to "Delete Market", @market, method: :delete, data:{confirm: "Are you sure?"}, class:"btn btn-danger btn-xs pull-right" %>
    </div>
  </div>
</div>

<% if @market.market_status_id == 1 %>
    <%= render 'form' %>
<% else %>
    <%= render 'form_once_published' %>
<% end %>

