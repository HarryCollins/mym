<% if @message.errors.any? %>
	
	$("#error_partial").html("<%= escape_javascript(render partial: 'shared/errors', locals: { obj: @message } ) %>")
    $("html, body").animate({ scrollTop: 0 }, "slow");
    $("#chat_text_area").val('');
    
<% end %>