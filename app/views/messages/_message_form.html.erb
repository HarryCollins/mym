<%= form_for message, remote: true do |f| %>
  <%= f.text_field :message_text, id: "chat_text_area" %>

  <%= f.hidden_field :market_id, value: market.id %>
  <%= f.submit "Send", class: "btn btn-primary", id: "submit_chat" %>
<% end %>